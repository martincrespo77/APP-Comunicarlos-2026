meta {
  name: Login Operador
  type: http
  seq: 1
}

post {
  url: {{base_url}}/usuarios/autenticar
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "operador@comunicarlos.com.ar",
    "password": "Pass1234!"
  }
}

docs {
  Obtiene un JWT para el usuario OPERADOR.
  Esperado: 200 {"access_token": "<jwt>", "token_type": "bearer"}
  → Copiar el access_token en la variable token_operador del entorno Local.
}
